# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: 02.sb-data-crud-methods
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_wipro
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
